version: '3.8'

services:
  app:
    build: .
    container_name: rimerx-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./static/audio:/app/static/audio
      - ./results:/app/results
    restart: unless-stopped
